# Copy to `.env` for local backend development (e.g. `vercel dev`).
# In production, set this in your hosting provider's environment
# variable settings instead of committing a .env file.
# Never expose this key to the mobile app or any client-side code.

API_FOOTBALL_KEY=
